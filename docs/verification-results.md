# سجل التحقق — QV-APP-001 / Run LOCAL-20260922-02

التاريخ: 22 سبتمبر 2026. السياق: فرع `fix/audit-critical` بعد commit
`5443814ac0dc7b6615b3d47ce8da4e4c18ed4088` مع تغييرات حزمة خطة الضبط.
المنفذ: جلسة الإصلاح الآلية. المراجع البشري وقرار الإصدار: **لم يُسجّلا**.
هذا سجل نتائج محلية، وليس محضر قبول إنتاج أو اعتماد QA.

| الفحص | النتيجة | الدليل القابل للإعادة |
|---|---|---|
| اختبارات التطبيق/Handler | 43 ناجحاً؛ 8 ملفات؛ صفر فشل | `npm test` |
| PostgreSQL/RLS | 13 ناجحاً؛ صفر فشل | `npm ci --prefix tools/database-tests --ignore-scripts` ثم `npm test --prefix tools/database-tests` |
| TypeScript | ناجح | `npx tsc --noEmit -p tsconfig.app.json` |
| ESLint للملفات المعدلة | ناجح | `BASE_SHA=de6d79d256f2bf078bcb76c1de244710335d796e node scripts/lint-changed.mjs` بعد تثبيت commit المرشح |
| بناء الإنتاج | ناجح مع تحذير bundle وتقادم بيانات Browserslist | `npm run build` |
| Syntax لأدوات Node | ناجح | `node --check scripts/lint-changed.mjs` و`node --check tools/database-tests/regression.test.mjs` |
| Audit أداة اختبار DB فقط | صفر تنبيهات | `npm audit --prefix tools/database-tests`؛ لا يشمل تبعيات التطبيق الأساسية |
| Schema الإنتاج — قراءة فقط | 0/6 من أعمدة NCR الجديدة؛ شرط الخروج المصحح غير موجود؛ 15 جدولاً بـRLS | استعلام metadata للبيئة المنشورة؛ يؤكد أن الإصلاح لم يُنشر |
| CI المستضاف | إعداد workflow أُضيف؛ النتيجة المرجعية من GitHub لكل SHA | صفحة Actions في PR #2؛ لا يُستبدل فحص GitHub بنتيجة محلية |
| Staging/UAT/Restore | لم تُنفذ | يجب تنفيذ G2/G3 من خطة الضبط |

## تفاصيل تغطية الاختبار

- SQL ينفّذ ملفات migrations الأصلية المحددة للجداول والسياسات قبل الإصلاح؛
  حسابان اصطناعيان ودور anon، دون بيانات مصنع أو أسرار.
- اختبار المحتوى NULL **يرفضه قيد documents الحقيقي**؛ ليس ادعاء بأن التطبيق
  يقبل مستنداً بلا نص. النص الفارغ مقبول على مستوى جدول البيانات في fixture،
  لكنه مرفوض من Endpoint، ويُختبر السلوكان كلٌ في طبقته.
- المستخدم B لا يقرأ أو يحذف مستند A، ولا يقرأ أجزاءه، ولا يُنشئ سجلاً بمالك A.
- مشغّل التقسيم يعمل عند INSERT المالك مع منع التنفيذ المباشر للدالة ومنع
  INSERT مباشر للأجزاء؛ UPDATE في اختبار الاستبدال إداري فقط.
- جسم طلب التحليل محدود فعلياً بـ8192 byte حتى عند تزوير Content-Length؛
  النص المخزن كامل بينما العرض يقتطع بعد 50000 حرف مع إشارة واضحة.
- لا replay كامل لتاريخ migrations المتعارض، ولا اختبار live Storage/Auth
  بحسابين، ولا native build أو Lighthouse في هذه الحزمة.

## القيود المفتوحة

1. لا إعلان إغلاق تدقيق الأمان أو جاهزية FSMS؛ الاعتماد الإلكتروني HACCP/CAPA مفتوح.
2. لا نشر قبل تحقق التوافق والنسخ/الاستعادة وUAT حسب الخطة.
3. PDF/Office متوقفان مؤقتاً في الإصلاح؛ إعادة التفعيل تتطلب parser واختبارات دقة.
4. CI يسجل npm audit الأساسي كدليل، ولا يعفي من معالجة التنبيهات الحالية.
5. لا منع مضمون لنسخ مستند مكرر إذا انقطعت الشبكة بعد commit ثم أعاد المستخدم الرفع؛ idempotency متابعة مطلوبة.
