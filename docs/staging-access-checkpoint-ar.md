# نقطة استئناف تجهيز Staging — 2026-09-22

PR #2 ما زال مفتوحاً كمسودة. مرشح الكود المعروف: `f8177825ce9ea7cf51aec18008c649799d99504c`. هذا تحديث أدلة فقط، وليس تطبيق migrations أو نشر الإصلاحات.

## أدلة الاتصال

- مشروع الإنتاج Lovable: `38eee966-71e9-4cb5-a060-26292b3e0c74`. نجح عليه `SELECT 1 AS connection_ok` عبر query_database. لم تُكتب بيانات إنتاج أثناء هذا الفحص.
- توجد نسخة قديمة غير منشورة باسم Remix of Alazwak app، معرفها `77c084ff-3b1d-428b-b165-00c4fb8be6d7`.
- get_database_status أعاد enabled=true للنسخة. ملف supabase/config.toml فيها يشير إلى `wuzasbgikzdvuqhejnqc`، وهو مختلف عن مرجع الإنتاج `lnchwlswprpegesupimg`.
- اختلاف ملف الإعداد لا يكفي لإثبات العزل الفعلي. فشل فحص أعداد Auth/Storage والجداول؛ وفشل SELECT 1 على النسخة برسالة `499 request_cancelled`. لا تُصنف النتيجة دليلاً على رفض الصلاحيات، ولا على خلو النسخة من البيانات.

## محاولة بديلة

- تأكد أن workspace `mgBjnIVXIzHkhFC7TI2H` على خطة free.
- جرت محاولة remix للمشروع الأصلي باسم Alazwak Staging PR2، دون نسخ المحادثة، عبر أداة Lovable المدعومة.
- رفضت الخدمة العملية برسالة صريحة بأن رصيد workspace نفد. رمز الغلاف INVALID_ARGUMENT، لكن السبب النصي نفاد الرصيد.
- أعيدت قراءة قائمة المشاريع: بقيت المشاريع الثلاثة الأصلية؛ لم يظهر مشروع Staging جديد.
- لم تُشترَ أرصدة، ولم تُغيّر خطة الاشتراك، ولم تُطبّق migrations على أي قاعدة في هذه الخطوة.

## الاستئناف المطلوب

1. عند إتاحة رصيد Lovable، افحص قائمة المشاريع قبل تكرار remix لمنع إنشاء نسخ مكررة.
2. إن لم يوجد Staging صالح، أنشئ نسخة ضمن الرصيد المتاح فقط، بلا ترقية أو شراء تلقائي.
3. تحقق فعلياً من عزل DB/Auth/Storage والاتصالات في الواجهة والوظائف؛ لا تعتمد على اسم المشروع أو ملف config وحده.
4. اتبع docs/approval-workflow-verification-ar.md لتطبيق migrations المختارة واختبارات API/Auth/RLS/MFA والاستعادة باستخدام بيانات اصطناعية.
5. يبقى الاعتماد مغلقاً في الإنتاج، ولا يتحول نجاح الاختبارات المحلية إلى إثبات نجاح سحابي أو قبول QA بشري.

العائق الحالي لا يتطلب إرسال Tokens أو تعديل إعدادات Privacy/MCP على الهاتف. قد يتيح التجديد الدوري رصيداً جديداً، لكن لم يُتحقق من استحقاق الحساب المتبقي هذا الشهر؛ لا يوجد موعد إنجاز مؤكد. أي تكلفة إضافية تحتاج عرضاً محدداً وموافقة المستخدم قبل الشراء.
